export enum LandingRoutes {
  ROOT = "/",
  PROFILE = "/profile",
  SETTINGS = "/settings",
  LOGOUT = "/logout",
}

export enum CredentialRoutes {
  OVERVIEW = "/credential",
  NEW = "/credential/new",
  SINGLE = "/credential/:id",
}
